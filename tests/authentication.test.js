const gsl = require('./../index');

/*
test('authentication', async () => {
  expect.assertions(1);
  try {
    gsl.credentials = {
      username: 'abc@domain.com',
      password: 'wrong password'
    }
    await gsl.authenticate();
  } catch (e) {
    expect(e).toEqual({
      error: 'Authentification to Google failed.',
    });
  }
});
*/
